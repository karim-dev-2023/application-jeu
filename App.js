import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View, SafeAreaView } from "react-native";

export default function App() {
  return (
    <SafeAreaView>
      <Text>Change code in the editor and watch it change on your phone§</Text>
    </SafeAreaView>
  );
}
